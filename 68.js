// 햄버거 만들기
function solution(ingredient) {
  let answer = 0;
  let count = 0;
  // 스택 배열 선언
  let temp = [];

  // ingredient 순회
  for (let i = 0; i < ingredient.length; i++) {
    temp.push(ingredient[i]);
  }

  return answer;
}
